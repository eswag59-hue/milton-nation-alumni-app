# Rendered intermediates — regenerated by src/clips/render_clip.mjs,
# src/*/render.mjs, make_titles.py, make_carousel.py, make_logo.py
build/
src/*/frames/
src/*/frames_*/
src/*/node_modules
src/*/*.log
src/*/pb_*.png
src/*/probe*.png
src/*/contact_*.jpg
# Finished renders are delivered directly; the sources regenerate them and a
# push once died on 540 committed frame PNGs.
assets/*.mp4
